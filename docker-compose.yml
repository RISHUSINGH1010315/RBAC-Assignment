version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://rishusingh2902_db_user:SIkCdwkaGdv99wxP@cluster0.1y0cazi.mongodb.net/rbac_task_db?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=supersecretkey123_rbac_task_manager_jwt_tokens
      - NODE_ENV=development
    restart: always

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: always
